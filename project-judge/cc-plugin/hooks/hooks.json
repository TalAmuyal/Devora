{
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/../../uv run --script -- ${CLAUDE_PLUGIN_ROOT}/main.py"
          }
        ]
      }
    ]
  }
}
